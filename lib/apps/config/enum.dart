enum Tema { system, dark, light }

enum LoremOpsi { words, sentences, paragraphs }

enum ModeHash { md5, sha1, sha256, sha512 }

enum ModeConverter { data, panjang }

enum UkuranData {
  bits,
  nibble,
  bytes,
  kilobits,
  kibibits,
  kilobytes,
  kibibytes,
  megabits,
  mebibits,
  megabytes,
  mebibytes,
  gigabits,
  gibibits,
  gigabytes,
  gibibytes,
}

enum UkuranPanjang {
  angstrom,
  nanometer,
  mikrometer,
  milimeter,
  sentimeter,
  meter,
  kilometer,
  inci,
  kaki,
  yard,
  mil,
  milLaut,
}
